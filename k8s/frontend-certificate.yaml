apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: derq-com-tls
  namespace: default
spec:
  secretName: derq-com-tls
  issuerRef:
    name: letsencrypt-prod
    kind: ClusterIssuer
  dnsNames:
    - derq.com
    - www.derq.com
