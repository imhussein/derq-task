# backend-certificate.yaml
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: api-derq-com
  namespace: default
spec:
  secretName: api-derq-com-tls
  dnsNames:
    - api.derq.com
  issuerRef:
    name: letsencrypt-prod
    kind: ClusterIssuer
